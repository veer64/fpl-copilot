data/*
# the FBref manual-export CSVs and sidecars are tracked (1.2 MB, 11 seasons): they are
# written from browser saves on the laptop and must reach the server with the code
!data/fbref/
data/fbref/_last_batch_report.json
.venv/
**/__pycache__/
*.pyc
.ipynb_checkpoints/

.env
github_actions_key
*.key
*_key

mlartifacts/
mlruns/
mlflow.db

fplcache/

deploy_key_new
deploy_key_new.pub
backups/
# The server deploy runbook. Scanned 2026-09-17: it contains NO credentials
# (no ntfy topic, no HEALTH_URL value, no private key, no password value).
# It is kept out because it is an operational map of a live host -- rotation
# status, sshd config precedence, the cron layout, and the note that the DB
# password passed through a chat -- and this repo is PUBLIC. The durable copy
# lives on disk at Handoffs/, untracked. See Logs/multi_run_schedule_design.md #13.
Handoffs/Server deploy handoff.docx

# saved browser pages for the manual FBref export (a megabyte each, not project data)
tmp/
